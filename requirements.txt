duckdb>=1.0.0
streamlit>=1.36
plotly>=5.22
pandas>=2.0
pyarrow>=15.0
# Optional: enables the agent's LLM narrative (Azure OpenAI / OpenAI). The agent
# runs fully without it. Uncomment to install.
# openai>=1.40
pulp>=2.8
